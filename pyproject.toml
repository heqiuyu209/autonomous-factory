[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "autonomous-factory"
version = "4.0.0"
description = "Autonomous Software Venture Factory - V2 core: problem statement to verified application via PM/Architect planning and task-graph driven factory"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy>=2.0",
    "pydantic>=2.5",
    "pydantic-settings>=2.2",
    "typer>=0.12",
    "PyYAML>=6.0",
    "psycopg2-binary>=2.9",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=4.1",
    "ruff>=0.6",
]
llm = [
    "openai>=1.30",
]

[project.scripts]
factory = "factory.cli:app"

[tool.setuptools.packages.find]
include = ["factory*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

# --- lint baseline -------------------------------------------------------
# Reproducible ruff config: default stable rule family (E4/E7/E9/F) plus
# isort. Line length is relaxed on purpose (docstrings/CLI text), verified
# green with: ruff check factory tests
[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

